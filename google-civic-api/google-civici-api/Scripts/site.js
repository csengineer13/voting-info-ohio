
// Map data to model
// mapper.fromJsToModel(data, this);

// Set up click listeners and other page bindings

// Create copy of "fresh" vm (for reverting)

// Setup router?

// Setup window location property on VM
// this.WindowLocation(window.location.pathname + window.location.hash);

// apply bindings

// remove hideUnbound class

// console.log